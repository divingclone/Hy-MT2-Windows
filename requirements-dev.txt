# CPU development/data tools. GPU runtime uses requirements-vllm-windows.txt.
# Versions used for this project's validation; install in a separate dev venv.
numpy==2.5.3
aiohttp==3.14.3
PyYAML==6.0.3
tqdm==4.70.1
huggingface-hub==1.32.0
# Public calibration-corpus preparation.
pyarrow==25.0.1
